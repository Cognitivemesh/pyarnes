# pyarnes-bench

Evaluation and benchmarking toolkit for the pyarnes agentic harness.

## Modules

| Module | Contents |
|---|---|
| `pyarnes_bench.eval` | `EvalResult`, `EvalSuite` |
| `pyarnes_bench.scorer` | `Scorer` ABC, `ExactMatchScorer` |

## Dependencies

- `pyarnes-core` — logging
